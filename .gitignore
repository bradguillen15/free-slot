# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Secrets — copy .env.example to .env locally
.env
.env.*
!.env.example
# Committed placeholder env for guest E2E (no real secrets — boots app in guest mode)
!.env.e2e
# Committed env for cloud E2E (local Supabase demo keys only — not secrets)
!.env.e2e-cloud
*.pem
*.key

# Playwright E2E
test-results/
playwright-report/
e2e/.cache/
# Cloud E2E: local Supabase status (URL + service-role key) captured at runtime
e2e/cloud/.supabase-status.json

# Supabase local stack artifacts
supabase/.branches
supabase/.temp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Graphify knowledge graph output (generated locally)
graphify-out/
.graphify_*.json

# Harness runtime paths
tmp/
.worktrees/
.mcp.json
**/.claude/settings.json
**/.claude/settings.local.json

# Local planning docs — kept on disk only, not published with the repo
docs/*-plan.md

# Placeholder Supabase config for guest-mode E2E tests.
# These are intentionally non-functional: the app boots into guest mode when
# the session request fails, so no real backend is contacted. Do NOT put real
# secrets here — this file is committed.
VITE_SUPABASE_URL=https://e2e.invalid
VITE_SUPABASE_PUBLISHABLE_KEY=e2e-placeholder-anon-key
VITE_SUPABASE_PROJECT_ID=e2e
